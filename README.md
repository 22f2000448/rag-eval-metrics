# rag-eval-metrics
Python package for evaluating RAG pipelines — measures faithfulness, relevancy, context precision &amp; context recall with sentence-transformer embeddings. CLI + LangChain integration. 500+ monthly downloads on PyPI
